schema: http://localhost:4000/graphql
documents: '../{client,shared}/*.gql' # targets the 'schema.gql' file in the shared folder and the 'get-user.query.gql' in the client folder
generates:
    ./graphql/generated.ts:
        plugins:
            - typescript
            - typescript-operations
            - typed-document-node
